import 'package:flutter/material.dart';
import 'package:iraq_bank/controller/textstyle.dart';

Widget c1 = Padding(
  padding: const EdgeInsets.all(8.0),
  child: Column(
    children: const [
      CircleAvatar(
        radius: 40,
        child: Icon(Icons.airplanemode_active_rounded, size: 40),
      ),
      Text("حجز طيران", style: CustomTextStyle.f16b),
    ],
  ),
);
//==================================================================

Widget c2 = Padding(
  padding: const EdgeInsets.all(8.0),
  child: Column(
    children: const [
      CircleAvatar(
        radius: 40,
        child: Icon(Icons.hotel, size: 40),
      ),
      Text("حجز فنادق", style: CustomTextStyle.f16b),
    ],
  ),
);
//==================================================================

Widget c3 = Padding(
  padding: const EdgeInsets.all(8.0),
  child: Column(
    children: const [
      CircleAvatar(
        radius: 40,
        child: Icon(Icons.luggage, size: 40),
      ),
      Text("تعليم", style: CustomTextStyle.f16b),
    ],
  ),
);
//==================================================================

Widget c4 = Padding(
  padding: const EdgeInsets.all(8.0),
  child: Column(
    children: const [
      CircleAvatar(
        radius: 40,
        child: Icon(Icons.favorite_border, size: 40),
      ),
      Text("صحة و تجميل", style: CustomTextStyle.f16b),
    ],
  ),
);
//==================================================================

Widget c5 = Padding(
  padding: const EdgeInsets.all(8.0),
  child: Column(
    children: const [
      CircleAvatar(
        radius: 40,
        child: Icon(Icons.travel_explore, size: 40),
      ),
      Text("انترنت", style: CustomTextStyle.f16b),
    ],
  ),
);
//==================================================================

Widget c6 = Padding(
  padding: const EdgeInsets.all(8.0),
  child: Column(
    children: const [
      CircleAvatar(
        radius: 40,
        child: Icon(Icons.shop, size: 40),
      ),
      Text("تسوق الكتروني", style: CustomTextStyle.f16b),
    ],
  ),
);
//==================================================================
Widget c7 = Padding(
  padding: const EdgeInsets.all(8.0),
  child: Column(
    children: const [
      CircleAvatar(
        radius: 40,
        child: Icon(Icons.keyboard, size: 40),
      ),
      Text("الكترونيات", style: CustomTextStyle.f16b),
    ],
  ),
);
//==================================================================
Widget c8 = Padding(
  padding: const EdgeInsets.all(8.0),
  child: Column(
    children: const [
      CircleAvatar(
        radius: 40,
        child: Icon(Icons.live_tv, size: 40),
      ),
      Text("القنوات التلفزيونية", style: CustomTextStyle.f16b),
    ],
  ),
);
//==================================================================
Widget c9 = Padding(
  padding: const EdgeInsets.all(8.0),
  child: Column(
    children: const [
      CircleAvatar(
        radius: 40,
        child: Icon(Icons.attractions, size: 40),
      ),
      Text("مناسبات وترفيه", style: CustomTextStyle.f16b),
    ],
  ),
);
//==================================================================
Widget c10 = Padding(
  padding: const EdgeInsets.all(8.0),
  child: Column(
    children: const [
      CircleAvatar(
        radius: 40,
        child: Icon(Icons.local_shipping_outlined, size: 40),
      ),
      Text("نقل وتوصيل", style: CustomTextStyle.f16b),
    ],
  ),
);
//==================================================================
Widget c11 = Padding(
  padding: const EdgeInsets.all(8.0),
  child: Column(
    children: const [
      CircleAvatar(
        radius: 40,
        child: Icon(Icons.verified_user_outlined, size: 40),
      ),
      Text("تأمين", style: CustomTextStyle.f16b),
    ],
  ),
);
//==================================================================
Widget c12 = Padding(
  padding: const EdgeInsets.all(8.0),
  child: Column(
    children: const [
      CircleAvatar(
        radius: 40,
        child: Icon(Icons.favorite_border, size: 40),
      ),
      Text("منظمات خيرية", style: CustomTextStyle.f16b),
    ],
  ),
);
//==================================================================
Widget c13 = Padding(
  padding: const EdgeInsets.all(8.0),
  child: Column(
    children: const [
      CircleAvatar(
        radius: 40,
        child: Icon(Icons.add_home_outlined, size: 40),
      ),
      Text("خدمات اعمال", style: CustomTextStyle.f16b),
    ],
  ),
);
//==================================================================
